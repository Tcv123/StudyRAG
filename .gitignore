tmp/
.tmp-pdfs/
node_modules/
.DS_Store
Thumbs.db
*.log
.claude/worktrees/

# Secrets — never commit
.env
.env.*
*.key
*.pem
*secret*
*credentials*
*api*key*
*api_key*
resend*
.pdfwork/

# Local runthrough harness — fakes user-scoped tables so a full student
# journey can be walked without an account. Never commit.
dev-fake-backend.js
