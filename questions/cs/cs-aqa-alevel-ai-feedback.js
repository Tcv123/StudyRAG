/*
 * AQA A-level Computer Science (7517) — AI Feedback Question Bank
 * Worded essay-style questions only, suitable for AI examiner-style marking.
 * Mark allocations: 6, 9, 12, 16, 20, 25. Command words: Describe / Explain /
 * Compare / Discuss / Assess / Evaluate / To what extent. Each question carries
 * a top-band model answer the AI can use as a grading anchor.
 */

const CS_AQA_AI_FEEDBACK = {

  /* ─────────────────────── 4.1 Fundamentals of Programming ─────────────────────── */
  '4.1': [
    {
      q: 'Describe the main data types available in a typical high-level programming language and explain why a programmer must declare them.',
      marks: 6, command: 'Describe',
      modelAnswer: 'Most high-level languages provide a small set of primitive data types: integer (whole numbers, e.g. 42), real or float (numbers with fractional parts, e.g. 3.14), Boolean (true/false), character (a single symbol such as \'A\'), and string (a sequence of characters such as "hello"). Many languages also provide date/time types and unsigned or fixed-precision numeric variants. A programmer must declare the data type so that the compiler or interpreter knows how much memory to allocate (a Boolean needs only one bit in principle, a 32-bit integer needs four bytes, a string needs variable storage), which binary representation to use (two\'s complement for signed integers, IEEE 754 for floats, ASCII or Unicode for characters) and which operations are valid (you cannot multiply two strings, but you can concatenate them). Declaring types also enables type checking, which catches many bugs at compile time rather than at run time and makes program intent clearer to future maintainers.'
    },
    {
      q: 'Explain the difference between a variable and a constant and the benefits of using each in a program.',
      marks: 6, command: 'Explain',
      modelAnswer: 'A variable is a named memory location whose value can change during program execution; a constant is a named value that is fixed at the point of declaration and cannot be reassigned. Variables are used wherever data must change over time — for example a loop counter, a running total, or a user input. Constants are used for values that are conceptually fixed for a given program — for example the value of pi, VAT rate, or the maximum size of an array. The benefits of constants are improved readability (MAX_USERS is clearer than the literal 1000 buried in code), single-point maintenance (changing one declaration updates every use) and safety (the compiler will reject any accidental attempt to reassign them). Variables provide the flexibility needed for almost all computation; constants reduce magic numbers and make program intent explicit.'
    },
    {
      q: 'Describe how selection structures such as IF, ELSE-IF and CASE/SWITCH are used to control the flow of a program.',
      marks: 9, command: 'Describe',
      modelAnswer: 'Selection structures let a program choose between alternative paths based on the truth of a Boolean expression. The simplest form is IF condition THEN statements END IF, which executes the block only when the condition evaluates to true. IF...ELSE adds an alternative path for the false case, and IF...ELSE IF...ELSE chains allow several mutually exclusive conditions to be tested in order, with the first true branch executed and the rest skipped. CASE (or SWITCH) takes a single expression and matches its value against a list of constant labels, executing the matching branch and optionally a default branch if none match — this is often clearer and more efficient than a long IF...ELSE IF chain when many discrete values are tested. Conditions can be combined using logical operators AND, OR and NOT, and nested selections allow more complex decision trees. Together these constructs let the same program take different actions for different inputs, which is essential for any non-trivial algorithm.'
    },
    {
      q: 'Explain the difference between iteration and recursion, illustrating with a simple example such as factorial.',
      marks: 9, command: 'Explain',
      modelAnswer: 'Iteration repeats a block of code using a loop construct such as FOR, WHILE or REPEAT-UNTIL; a counter or condition controls the number of repetitions. Recursion is when a subroutine calls itself with a smaller version of the original problem, terminating at a base case. Factorial is a classic illustration. Iteratively: set result = 1, then FOR i = 1 to n DO result = result × i; this uses constant memory and a simple loop. Recursively: factorial(n) returns 1 if n ≤ 1 (the base case), otherwise it returns n × factorial(n-1). Each recursive call adds a new frame to the call stack containing its local variables and return address; if the base case is missing or never reached, the stack overflows. Recursion often expresses naturally recursive problems (tree traversal, divide-and-conquer, Tower of Hanoi) more elegantly than iteration, but it uses more memory and has function-call overhead. Iteration is usually faster and more space-efficient. Many compilers optimise tail-recursive calls into iteration, but in general the two are interchangeable: any iteration can be rewritten as recursion and vice versa.'
    },
    {
      q: 'Compare passing parameters by value with passing parameters by reference.',
      marks: 9, command: 'Compare',
      modelAnswer: 'When a parameter is passed by value, a copy of the argument is placed into the subroutine\'s local variable; changes inside the subroutine do not affect the original. When passed by reference, the subroutine receives the memory address of the original variable, so any changes inside the subroutine affect the original. Pass-by-value is safer because subroutines cannot accidentally alter caller data, supporting modular reasoning — the subroutine behaves like a mathematical function whose only effect is its return value. However, copying large objects is slow and memory-hungry, so for arrays or records pass-by-reference is often preferred for efficiency. Pass-by-reference allows a subroutine to return several values via its parameters (useful where multiple return values are needed), but increases the risk of side-effects and tightens coupling between caller and callee. Many languages mix the two: small primitives default to by-value, while arrays and objects are passed by reference or via an implicit pointer. Some languages also provide read-only references that combine the efficiency of references with the safety of values.'
    },
    {
      q: 'Discuss the use of subroutines in structured programming and assess their advantages.',
      marks: 12, command: 'Discuss',
      modelAnswer: 'A subroutine is a named, self-contained block of code (a procedure or a function) that can be called from elsewhere in the program. Procedures perform an action; functions return a value. In structured programming, a large problem is decomposed into smaller subproblems, each implemented as a subroutine; this top-down design produces a hierarchy of subroutines that are individually small enough to understand, test and debug. The main advantages are reuse (one definition, many calls — reducing code duplication and the bugs that come with it), abstraction (callers need to know what a subroutine does, not how — so internal changes do not break callers), readability (well-named subroutines turn code into a series of meaningful steps), and easier testing because each subroutine can be tested in isolation. Subroutines also encourage scope discipline: local variables prevent unintentional interference between parts of the program, and parameter lists make data dependencies explicit. Modern teams divide work along subroutine boundaries so several developers can work in parallel. The drawbacks are minor: each call has a small overhead for stack manipulation, and excessive fragmentation into trivial subroutines can hurt readability rather than help it. On balance, subroutines are the foundation of every non-trivial program; without them, large software would be unmaintainable.'
    },
    {
      q: 'Explain the role of local and global variables, and discuss why local variables are generally preferred.',
      marks: 12, command: 'Explain',
      modelAnswer: 'A local variable is declared inside a subroutine and exists only while that subroutine is executing — it lives on the call stack and is destroyed when the subroutine returns. A global variable is declared outside any subroutine and is accessible to every subroutine throughout the program for as long as the program runs. Global variables are convenient when many subroutines need access to the same state (e.g. a counter or configuration value), but they create hidden dependencies: any subroutine can modify them, so a bug in one part of the program can corrupt data used in another, and tracing the cause requires inspecting the whole code base. They also make subroutines harder to test in isolation (the behaviour depends on global state, not just the arguments) and obstruct concurrent execution because two threads may compete for the same global. Local variables, by contrast, are private to one subroutine: they cannot be changed unexpectedly, they support reuse because the same name can be used in different subroutines without conflict, they enable recursion (each call has its own frame), and they make subroutines easier to reason about because all inputs are visible in the parameter list. Best practice is therefore to use local variables wherever possible and to pass shared data explicitly through parameters or return values, reserving globals for genuine program-wide constants such as configuration.'
    },
    {
      q: 'Compare procedural and object-oriented programming paradigms.',
      marks: 12, command: 'Compare',
      modelAnswer: 'Procedural programming organises a program around procedures (subroutines) that operate on data; data and behaviour are separate, and the structure is typically a top-down hierarchy of procedures decomposing one main task. Object-oriented programming (OOP) organises a program around objects, each of which bundles related data (attributes) and behaviour (methods) together; a class is a template from which objects are instantiated. Both paradigms support reuse, but in different ways: procedural reuse is via subroutine libraries, OOP reuse is via inheritance (deriving new classes from existing ones), composition and polymorphism (one interface, many implementations). OOP enforces encapsulation: object internals are hidden behind a public interface, reducing accidental coupling between modules. Procedural code can achieve the same goal but only by convention. OOP fits naturally where the problem domain consists of clearly identifiable entities (employees, accounts, vehicles), and supports GUI frameworks, simulations and large team-based projects well. Procedural code is often simpler for small, computational tasks and may have lower run-time overhead because it avoids virtual dispatch. Modern languages such as Python, C++ and C# mix both: most professional software uses procedural code inside object methods. Neither is universally superior — the choice depends on the size and shape of the problem.'
    },
    {
      q: 'Assess the use of strongly typed languages in producing reliable software.',
      marks: 12, command: 'Assess',
      modelAnswer: 'A strongly typed language enforces type rules strictly: a variable\'s type cannot be reinterpreted, implicit conversions are limited, and operations that mix incompatible types are rejected. Examples include Java, C#, Haskell and Rust; weakly typed languages include classic C and many scripting languages. The main benefit for reliability is that many programming errors are caught at compile time rather than at run time — a misspelt field name, a missing argument, a string accidentally passed where an integer is required will all be reported before the program is shipped, when they are cheap to fix. Strong typing supports refactoring (the compiler flags every place affected by a change), enables better tooling (autocomplete, jump-to-definition, automated refactoring), and serves as living documentation of intent. It also reduces the surface area for certain security vulnerabilities such as buffer overflows or unintended pointer arithmetic. However, strong typing can slow initial development because the programmer must annotate types, and overly rigid type systems can force awkward workarounds (generic containers, casts) in real code. Weakly typed languages allow rapid prototyping and concise expression but rely on disciplined testing to catch type-related bugs. On balance, for large, long-lived, safety-critical software, strong typing significantly improves reliability; for small scripts and exploratory work the trade-off is less clear, which is why hybrid approaches (gradual typing, type hints in Python and TypeScript) have become popular.'
    },
    {
      q: 'Discuss the principles of encapsulation, inheritance and polymorphism in object-oriented programming.',
      marks: 16, command: 'Discuss',
      modelAnswer: 'These three principles are central to OOP. Encapsulation means bundling data (attributes) and the methods that operate on it into a single class, and restricting outside access using visibility modifiers (private, protected, public). The class exposes a public interface and hides its internal representation, so the implementation can change without affecting callers. This reduces coupling, prevents invalid states (e.g. a BankAccount class enforces that the balance is never set negative) and makes large programs maintainable. Inheritance allows a new class (subclass) to be defined as a specialisation of an existing class (superclass), inheriting its attributes and methods. The subclass can override or add behaviour. Inheritance models is-a relationships (a SavingsAccount is a BankAccount) and promotes code reuse. Overused, it can produce fragile hierarchies where changes to a superclass break subclasses, which is why many modern designs favour composition over inheritance for shared behaviour. Polymorphism means the same method call behaves differently depending on the actual object type. A common form is method overriding: a subclass redefines a method declared in its superclass, and the runtime dispatches the call based on the dynamic type, not the static type. This lets a single line such as account.calculateInterest() work correctly across a collection of mixed BankAccount, SavingsAccount and ISAAccount objects without conditional logic. Polymorphism, especially when combined with abstract classes or interfaces, enables open-closed design: existing client code does not change when new subclasses are added. Together, encapsulation, inheritance and polymorphism let programmers build large systems that are modular, extensible and resilient to change — provided they are used judiciously, since deep hierarchies and over-engineered abstractions can also harm clarity.'
    },
    {
      q: 'Evaluate the use of exception handling in producing robust programs.',
      marks: 16, command: 'Evaluate',
      modelAnswer: 'Exception handling allows a program to detect a run-time error (such as division by zero, a missing file or invalid input) and respond gracefully rather than crashing. The exception is raised at the point of failure and propagates up the call stack until it is caught by an enclosing handler (try-catch in Java/C#/Python). The handler can log the error, retry the operation, fall back to a default, or display a meaningful message to the user. The benefits for robustness are substantial. First, exception handling separates the "happy path" of normal logic from the messy details of error handling, leaving the main flow readable. Second, errors cannot be silently ignored: an uncaught exception will terminate the program with a stack trace, making bugs visible during testing. Third, it enables defensive design at trust boundaries — for example, validating user input or network responses without sprinkling error checks throughout business logic. Fourth, custom exception classes carry semantic information that callers can use to react differently to different failures. However, exceptions are not a panacea. They can be misused for normal control flow, which both slows the program (raising and catching exceptions is expensive) and hides intent. Catch-all handlers that swallow every exception mask bugs and degrade reliability rather than improving it. Resource leaks can occur if a function exits unexpectedly without releasing file handles or locks — modern languages mitigate this with try-with-resources or context managers. Finally, exception handling does not replace input validation: it should be the last line of defence, not the first. Used carefully — narrow catch blocks, specific exception types, proper cleanup — exception handling is one of the strongest tools for producing robust software; used carelessly, it can do more harm than good.'
    },
    {
      q: 'Explain how built-in functions, libraries and APIs support efficient program development.',
      marks: 16, command: 'Explain',
      modelAnswer: 'Built-in functions are routines that come with the language itself (e.g. len(), print(), str()); standard libraries are larger collections of related modules shipped with the language for common tasks such as file I/O, networking and data structures; and APIs (Application Programming Interfaces) are public sets of operations exposed by external libraries, frameworks or remote services. They support efficient development in several ways. Reuse: a programmer does not have to reimplement well-known algorithms such as sorting, hashing or JSON parsing, saving time and avoiding bugs. Quality: standard libraries have been tested and optimised by experts and many users, so they tend to be faster and more reliable than ad-hoc reinventions. Portability: code that uses standard libraries usually runs across operating systems with little change, because the library hides platform differences. Abstraction: a higher-level API lets the programmer think in terms of business concepts (HTTPS request, JSON document, database row) rather than low-level byte manipulation. Composition: APIs are designed to fit together, so complex behaviour can be assembled from simple parts. Speed of iteration: a small change to a few lines of API-using code often replaces what would otherwise be hundreds of lines. There are trade-offs. Dependencies can become security and maintenance liabilities: a vulnerability in a popular library affects every program that uses it (supply-chain risk). Excessive dependence on third-party APIs ties the program to their version cycle and to the vendor\'s business decisions. Overuse of obscure libraries hurts code clarity if other developers must learn each one. Best practice is therefore to prefer the standard library where possible, choose well-maintained external libraries with permissive licences, and avoid pulling in heavy dependencies for trivial tasks.'
    },
    {
      q: 'Discuss the strengths and weaknesses of object-oriented programming for large-scale software development.',
      marks: 20, command: 'Discuss',
      modelAnswer: 'Large-scale software is characterised by long lifetimes, many developers, evolving requirements and complex domains; OOP was designed largely to address these forces. Strengths. Encapsulation hides implementation behind interfaces, so teams can work on different classes in parallel and change internals without breaking callers. Inheritance and composition support reuse, especially through frameworks (Java EE, .NET, Spring) where the framework provides large amounts of behaviour that the application customises by subclassing or implementing interfaces. Polymorphism enables open-for-extension/closed-for-modification design: new types can be plugged in without rewriting old code. OOP maps naturally onto domain modelling: a banking system has Account, Customer, Transaction classes that mirror the business; non-programmers can often follow class diagrams. OOP also fits well with design patterns (Observer, Strategy, Factory) that capture proven solutions to recurring large-system problems. Tool support is mature — IDEs, refactoring tools, profilers and testing frameworks all assume OOP code, and team conventions are well established. Weaknesses. OOP introduces overhead: classes, getters, setters, factories and interfaces can balloon a small problem into many files, hurting readability. Deep inheritance hierarchies are fragile: changing a superclass can ripple through subclasses in unforeseen ways (the "fragile base class" problem), which is why composition is now often preferred. OOP can encourage mutable shared state, which becomes hard to reason about in concurrent systems where multiple threads operate on the same objects; functional and immutable-data approaches can be safer here. Some problem domains (numerical computing, low-level systems) do not map naturally to objects, and forcing them into classes adds clutter. Modelling errors made early in design (wrong class boundaries, premature abstraction) are expensive to correct later. Overall, OOP remains the dominant paradigm for large applications because its trade-offs match the needs of long-lived projects: maintainability and team scalability outweigh its overheads. Modern best practice mixes OOP with functional and procedural styles, picks the right abstraction level for each component, and prefers composition over inheritance — getting the benefits of OOP while avoiding the worst of its pitfalls.'
    },
    {
      q: 'Evaluate how design choices in subroutines, scope and parameter passing affect program maintainability.',
      marks: 20, command: 'Evaluate',
      modelAnswer: 'Maintainability is the ease with which a program can be understood, corrected and extended over its lifetime; small day-to-day design choices have a much larger cumulative effect on it than any single architectural decision. Subroutines. Short, single-purpose subroutines are easier to read, test and reuse than long ones; if a routine cannot fit on a single screen or its purpose cannot be summed up in a sentence, it is usually doing too much and should be split. Meaningful names (calculateNetSalary, not doStuff) and consistent ordering of parameters reduce the cognitive load on every future reader. However, hyper-fragmentation into one-line subroutines harms readability — every call becomes an indirection. Subroutines should be cohesive: everything they do should serve one clear purpose. Scope. Restricting variables to the smallest scope possible (local where you can, parameters where you must, globals only as a last resort) limits the part of the program that can interact with each piece of state and therefore the surface area for bugs. Using constants in place of magic numbers also reduces maintenance cost because a value only needs to change in one place. Modern languages support module-level visibility (private classes, package-private members) which lets a designer expose a stable public API while freely refactoring the internals — a powerful maintainability lever. Parameter passing. Explicit parameters make data dependencies visible and testing easier: a subroutine that takes everything it needs as arguments behaves the same in any context. Pass-by-value is safest because the subroutine cannot mutate caller data, but it is impractical for large objects. Pass-by-reference is efficient but invites side-effects unless used with discipline (read-only references, immutable types). Functions that return values rather than mutate parameters tend to compose better and lend themselves to functional-style chains. Overly long parameter lists are a code smell; they usually signal that a related set of values should be grouped into a record or class. Together, these decisions reinforce one another: small, cohesive subroutines naturally have short parameter lists, narrow scope and few side-effects, leading to a code base where any developer can confidently change a single routine without tracing far-reaching consequences. Careless choices — long subroutines, sprawling globals, ad-hoc reference parameters — compound into systems where every change is risky, tests are brittle, and onboarding new developers is slow.'
    },
    {
      q: 'To what extent does the choice of programming paradigm determine the success of a large software project?',
      marks: 25, command: 'To what extent',
      modelAnswer: 'The paradigm — procedural, object-oriented, functional, event-driven or a mix — sets the high-level vocabulary in which the team will think, design and communicate. It influences how code is organised, how data and behaviour are bundled, how change is absorbed and how new developers learn the system. To that extent it is undeniably important. However, the empirical record of large software projects shows that paradigm is only one factor among many, and rarely the dominant one. Where paradigm matters. OOP\'s emphasis on encapsulation and polymorphism makes it well suited to long-lived business applications with rich domain models; banks, insurers and ERP vendors have built massive systems in Java and C# because the paradigm scales to many developers working on shared code over decades. Functional programming\'s emphasis on immutability and pure functions reduces concurrency bugs and is increasingly used in high-throughput data systems (Scala/Akka, Erlang/OTP, Clojure) where mutable shared state would be a disaster. Event-driven and reactive paradigms suit user interfaces and real-time systems where the program responds to a stream of inputs. Picking a paradigm that fights the problem — for example, forcing pure functional style on a UI-heavy CRUD application, or building a high-volume real-time pipeline with mutable global state — creates friction that no amount of skill will entirely overcome. Where paradigm matters less. Project failure surveys (e.g. CHAOS reports, Standish Group) consistently identify the leading causes as unclear or changing requirements, weak project management, communication breakdowns, unrealistic schedules, lack of user involvement and inadequate testing. These causes are paradigm-agnostic: a well-run procedural project will outperform a poorly run OOP project, and vice versa. Tool support, team experience and culture matter as much as paradigm. A team fluent in OOP will produce better OOP code than a similar team forced into a paradigm they barely know; the cost of switching paradigms midway is substantial. Modern languages also blur the lines: Python, JavaScript and Kotlin support several paradigms at once, so a single project routinely uses procedural code inside object methods which themselves use higher-order functions. Finally, factors outside coding — choice of architecture (monolith vs microservices), deployment platform, observability, security practices and DevOps maturity — often have larger effects on the project\'s outcome than the choice between, say, Java and Scala. Conclusion. The paradigm is a meaningful constraint and a poor choice can hurt, but it is far from determinative. A great team, clear requirements, disciplined engineering practices and a paradigm reasonably suited to the problem will almost always succeed; an absent or untrained team, vague requirements or a chaotic process will fail no matter what paradigm is chosen. The right way to phrase the conclusion is therefore that paradigm matters at the margin: it can amplify a team\'s strengths or weaknesses, but it is rarely the deciding factor in whether a large software project succeeds.'
    },
  ],

};
